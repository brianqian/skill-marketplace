ktor {
    deployment {
        port = 8080
        port = ${?PORT}

    }
    application {
        modules = [ org.covid19support.ApplicationKt.main,
                    org.covid19support.modules.users.RoutesKt.users
                  ]
    }
}